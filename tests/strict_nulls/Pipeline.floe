-- Test that non-Maybe columns reject nulls at runtime

schema Input {
    name: String,
    age: Int,
}

schema Output {
    name: String,
    age: Int,
}

let transform : Input -> Output = select [name, age]

main = read "input.parquet" as Input |> transform sink "output.parquet"
