-- @compiles
schema Input {
    old_name: String,
    other: Int64,
}

schema Output {
    new_name: String,
    other: Int64,
}

fn renameCol :: Input -> Output
fn renameCol =
    rename old_name new_name
